<div class="container favorites-container">
  <div class="favorites-header">
    <h1>⭐ お気に入り一覧</h1>
    <p class="subtitle">保存した名言と占いをチェック</p>
  </div>
  
  <div class="stats-section">
    <h2>統計情報</h2>
    <div class="stats-grid">
      <% stats.forEach(stat => { %>
        <div class="stat-card">
          <div class="stat-icon">
            <%= stat.type === 'quote' ? '💭' : '⭐' %>
          </div>
          <div class="stat-label">
            <%= stat.type === 'quote' ? '名言' : '占い' %>
          </div>
          <div class="stat-value"><%= stat.count %> 件</div>
          <div class="stat-likes">❤️ <%= stat.total_likes %></div>
        </div>
      <% }); %>
    </div>
  </div>
  
  <div class="filter-section">
    <label>フィルター:</label>
    <select id="type-filter" onchange="location.href='/favorites?type=' + this.value">
      <option value="" <%= selectedType === 'all' ? 'selected' : '' %>>すべて</option>
      <option value="quote" <%= selectedType === 'quote' ? 'selected' : '' %>>名言のみ</option>
      <option value="horoscope" <%= selectedType === 'horoscope' ? 'selected' : '' %>>占いのみ</option>
    </select>
  </div>
  
  <div class="favorites-list">
    <% if (favorites.length === 0) { %>
      <div class="empty-state">
        <div class="empty-icon">📭</div>
        <p>まだお気に入りがありません</p>
        <p class="empty-hint">名言や占いページで「お気に入りに追加」をクリックしてください</p>
      </div>
    <% } else { %>
      <% favorites.forEach(fav => { %>
        <div class="favorite-card" data-id="<%= fav.id %>">
          <div class="favorite-type">
            <%= fav.type === 'quote' ? '💭 名言' : '⭐ 占い' %>
          </div>
          <div class="favorite-content"><%= fav.content %></div>
          <% if (fav.author) { %>
            <div class="favorite-author">— <%= fav.author %></div>
          <% } %>
          <div class="favorite-footer">
            <div class="favorite-likes">❤️ <%= fav.likes %></div>
            <div class="favorite-date">
              <%= new Date(fav.created_at).toLocaleDateString('ja-JP') %>
            </div>
            <button class="delete-btn" onclick="deleteFavorite(<%= fav.id %>)">
              🗑️ 削除
            </button>
          </div>
        </div>
      <% }); %>
    <% } %>
  </div>
  
  <div class="nav">
    <a href="/">ホーム</a>
    <a href="/date">今日の日付</a>
    <a href="/horoscope">星座占い</a>
    <a href="/quote">偉人の名言</a>
  </div>
</div>

<script>
  async function deleteFavorite(id) {
    if (!confirm('このお気に入りを削除しますか？')) return;
    
    try {
      const response = await fetch(`/api/favorites/${id}`, {
        method: 'DELETE'
      });
      
      if (response.ok) {
        location.reload();
      } else {
        alert('削除に失敗しました');
      }
    } catch (err) {
      console.error('Error:', err);
      alert('削除に失敗しました');
    }
  }
</script>

<style>
  .favorites-container {
    max-width: 900px;
    padding: 40px;
  }
  
  .favorites-header {
    text-align: center;
    margin-bottom: 40px;
  }
  
  .favorites-header h1 {
    font-size: 36px;
    margin-bottom: 10px;
  }
  
  .subtitle {
    color: #666;
    font-size: 16px;
  }
  
  .stats-section {
    margin-bottom: 40px;
  }
  
  .stats-section h2 {
    font-size: 20px;
    margin-bottom: 20px;
    color: #333;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    margin-bottom: 30px;
  }
  
  .stat-card {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
    padding: 25px;
    border-radius: 15px;
    text-align: center;
    border: 2px solid rgba(102, 126, 234, 0.3);
  }
  
  .stat-icon {
    font-size: 40px;
    margin-bottom: 10px;
  }
  
  .stat-label {
    font-size: 14px;
    color: #666;
    margin-bottom: 10px;
  }
  
  .stat-value {
    font-size: 28px;
    font-weight: 700;
    color: #667eea;
    margin-bottom: 5px;
  }
  
  .stat-likes {
    font-size: 14px;
    color: #999;
  }
  
  .filter-section {
    margin-bottom: 30px;
    display: flex;
    align-items: center;
    gap: 10px;
  }
  
  .filter-section label {
    font-weight: 600;
    color: #555;
  }
  
  .filter-section select {
    padding: 10px 15px;
    border: 2px solid #667eea;
    border-radius: 20px;
    font-size: 14px;
    cursor: pointer;
    background: white;
  }
  
  .favorites-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin-bottom: 40px;
  }
  
  .favorite-card {
    background: white;
    padding: 25px;
    border-radius: 15px;
    border: 2px solid #e0e0e0;
    transition: all 0.3s;
  }
  
  .favorite-card:hover {
    border-color: #667eea;
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.2);
  }
  
  .favorite-type {
    font-size: 14px;
    color: #667eea;
    font-weight: 600;
    margin-bottom: 15px;
  }
  
  .favorite-content {
    font-size: 18px;
    color: #333;
    line-height: 1.6;
    margin-bottom: 15px;
  }
  
  .favorite-author {
    font-size: 16px;
    color: #667eea;
    font-weight: 600;
    margin-bottom: 15px;
  }
  
  .favorite-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 15px;
    border-top: 1px solid #e0e0e0;
  }
  
  .favorite-likes {
    font-size: 14px;
    color: #e74c3c;
    font-weight: 600;
  }
  
  .favorite-date {
    font-size: 12px;
    color: #999;
  }
  
  .delete-btn {
    background: #e74c3c;
    color: white;
    border: none;
    padding: 8px 15px;
    border-radius: 20px;
    cursor: pointer;
    font-size: 12px;
    transition: all 0.3s;
  }
  
  .delete-btn:hover {
    background: #c0392b;
    transform: translateY(-2px);
  }
  
  .empty-state {
    text-align: center;
    padding: 60px 20px;
    color: #999;
  }
  
  .empty-icon {
    font-size: 80px;
    margin-bottom: 20px;
  }
  
  .empty-state p {
    font-size: 18px;
    margin: 10px 0;
  }
  
  .empty-hint {
    font-size: 14px;
    color: #bbb;
  }
</style>
